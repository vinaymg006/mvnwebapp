---
  -hosts:webservers
   become:true
   tasks:
     -name:start container
      docker_container:
        name:mvnwebapp
        image:"vinaymg006/webapp:${BUILD_NUMBER}"
        state:started
        published_ports:
          - 0.0.0.0:8080:8080


